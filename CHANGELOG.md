# 更新日志

本文件记录项目所有值得注意的改动。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.0] - 2026-09-23

自 v0.1.0 起，检索对象从"物品"扩展为**六个实体域**，并补齐了数据管理页、图标归档读取与界面可用性。

### 新增

- **实体域由 3 个扩展到 6 个**：`item` / `npc` / `mob` / `skill` / `morph` / `reactor`。
  六域共用同一条解析管线，各入一张同构表，靠 `(lang, 实体域, 8 位零填充 ID)` 隔离——
  实测 `Mob.wz`、`Npc.wz` 的 ID 与物品 ID 分别重叠 1,861 / 1,806 个，合并成一张表必然互相覆盖。
  - **NPC / 怪物**：名称 `String.wz/{Npc,Mob}.img.xml`，属性 `Npc.wz`、`Mob.wz`；
  - **技能**：名称 `String.wz/Skill.img.xml`，属性 `Skill.wz/<组>.img.xml`——技能 ID 藏在 `skill/` 再下一层，
    节点没有 `info` 子目录，等级数值取自 `level/1` 并加 `level1.` 前缀；
  - **变身 / 反应堆**：**没有名称表**（`String.wz` 无对应表，`Morph.wz` 71 个文件无一含 `name`，
    地图数据里的 reactor 节点是空壳），名称整列显示「缺名称」；反应堆的韩文 `info/info` 按普通属性入库，不混进名称。
- **数据管理页 `/admin`**：四个标签页——比对与重载（只读比对磁盘指纹与库内指纹，列新增/变更/已删除并批量重载）、
  文件记录（按路径检索、单文件强制重载）、实体修正（按域检索并手工改名称/描述/分类/属性 JSON）、扫描历史。
  写接口受 `admin.token` 与本机回环双重闸门，`admin.enabled=false` 时页面与端点整体 404。
- **手工修改保护**：改过的行打「手工」标记，重载时默认跳过，避免手工订正被下一次扫描覆盖。
- **图标可直接读归档**：`icons.dir` 除目录外还支持指向打包好的 `imgdata.zip`（Store 模式按 `DataOffset` 零拷贝直读，
  deflate 条目自动退回解压；启动只解析中央目录）。换 zip 只需重启 `serve`，图标不落库。
- **白天 / 夜晚主题切换**：页头右上角按钮，配色只改 CSS 变量，选择存 `localStorage`，
  检索页与管理页共用同一个键；首屏在 `<head>` 里提前套用，暗色下不闪白。
- **首页右上角「管理页」入口**：按 `/api/stats` 的 `adminOn` 自动显隐。
- **详情浮层显示数据来源**：标明这条记录的**名称来自哪个 `String.wz` 表、属性来自哪个文件**，改完 XML 就知道该重载谁。
- **CLI 与 HTTP 全面带实体域参数**：`query/stats -kind=`、`/api/{items,item,meta,stats,langs}?kind=`、
  `/img/<id>.png?for=<kind>`。不带 `kind` 的旧链接与脚本仍等价于 `kind=item`。
- 分类与属性中文别名字典按域分开（`undead` 在装备上是"对不死族伤害"，在怪物身上是"不死族属性"）。

### 变更

- 列表区改为**固定高度卡片**：内部滚动、表头吸顶、分页条钉在卡片底部——翻页不必把页面滚到底。
- 「关键属性」列限宽可换行，**最多 3 行**，超出截断为「…另 N 项」，全量看详情浮层。
- 分页条页码形态改为 `1 … 当前页前后各 5 页 … 末页`。
- 落库定序与并发到达顺序解耦：同一 ID 被多个文件写入时，胜者由 `(ID, 来源路径)` 定序决定，不再取决于哪个协程先跑完。
- 文件溯源 `wz_file.ids` 改存 `kind:id`（裸 ID 仍按物品解释，向后兼容）。
- 默认参与解析的包由 5 个增至 8 个（新增 `Skill.wz`、`Morph.wz`、`Reactor.wz`）。

### 修复

- **名称侧跨实体污染**：`String.wz` 20 张表里只有 9 张带 `name`，其中怪物/NPC/技能表的键补零后与物品 ID 大量重叠，
  早先整目录收名称造成 3,791 行污染；改为**表名白名单**定域，并按"有名称却无任何溯源认领"判据一次性清污。
- `Mob.wz/QuestCountGroup/` 那 10 个"怪物号 → 任务计数"查表文件被当成怪物属性收进来；
  非物品域现在要求实体文件**紧贴包根一层**。
- `String.wz/Eqp` 里发型/脸型用 **5 位 ID**，导致 22,691 条 Hair/Face 名称整批丢弃；对该表单独放行 5 位。
- `Item.wz/Pet`、`Mob.wz`、`Npc.wz` 的"一文件一实体"未被认出，461 条宠物道具属性整批漏抽；
  `RootID` 判据由"只看位数"改为"文件名与根 imgdir 名同为数字 ID"。
- `Character.wz` 根级 18 个文件没有类别目录，分类为空；按 `info.islot` 归为 `BodySkin` / `HeadSkin`。
- **管理页偶发滚不动**：动态重绘表格时 Chromium 的滚动锚定补偿 `scrollY`，表现为滚不动或回弹、重新加载才恢复；
  以 `html { overflow-anchor: none }` 关闭整页锚定。
- 图标按域取用，修掉"该 ID 没有装备图就拿 NPC 立绘顶上"的串味（原 22,162 行命中里 1,578 行来自 Npc 域）。

### 性能

- 图标 zip 归档：37,109 张建索引约 75ms，单张读取 58µs（同内容目录方式 598µs）。

### 数据基线（`zh-CN`，2026-09-23 全量重扫）

| 实体域 | 条数 | 有名称 | 有属性 | 齐全 |
|---|---|---|---|---|
| 物品 `item` | 52,558 | 46,628（88.72%） | 51,888（98.73%） | 45,958 |
| NPC `npc` | 7,474 | 1,842（24.65%） | 6,208（83.06%） | 576 |
| 怪物 `mob` | 2,532 | 2,034（80.33%） | 2,379（93.96%） | 1,881 |
| 技能 `skill` | 544 | 542（99.63%） | 536（98.53%） | 534 |
| 变身 `morph` | 71 | 0 | 71（100%） | 0 |
| 反应堆 `reactor` | 462 | 0 | 462（100%） | 0 |

已记录文件 56,106 个，解析失败 0；图标 37,109 张 / 36,646 个可用 ID，六域均可取图。

### 破坏性变更

- 数据库新增 `skill` / `morph` / `reactor` 三张表，由 `store.Open` 自动建表，**旧库无需重建**；
  但要看到新域数据必须重跑一次 `scan`。
- 本机 `wzconfig.json` 的 `icons.dir` 由 `imgdata` 目录改为 `imgdata.zip`（纯配置项，改回即退回目录语义）。
- `.gitignore` 增补 `/imgdata.zip`：图标归档与所有游戏素材同样**不随仓库分发**。

### 升级提示

```bash
go build -o bin/maplewzmeta.exe ./cmd/maplewzmeta   # 必须先用新二进制再重扫
maplewzmeta scan -force -overwrite                  # 建表 + 按新域分流
maplewzmeta serve
```

> ⚠️ 改完 `extract` / `store` 的域规则后**务必先重编译再 `scan`**。本项目曾用旧二进制重扫，
> 三个新包被旧 `KindOf` 判成 `item` 域、把 420 个反应堆 ID 写进物品表，
> 靠 `scan -force -overwrite` 与手工删除空壳行才回到基线（详见 `docs/09` §2）。

## [0.1.0]

首个开源版本：WZ 导出 XML 解析、物品 ID 关联、SQLite 存储、CLI 检索与内置查询页。
